### A quick sidenote

the examples uses [nob](https://github.com/tsoding/nob.h) as the "buid system" but you can also use makefiles (nob is the goat).
Also I recommend you checkout the wallpaper example first because it has some handy comments.
